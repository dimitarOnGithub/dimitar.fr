# TLS configuration as per:
# https://ssl-config.mozilla.org

# IMPORTANT:
# The configuration in this file MUST match the Nginx and OpenSSL version
# inside of the proxy container. Use pinned version to avoid version<->config drift

# Configuration applicable to image:
# nginx:1.29.8
# nginx version: nginx/1.29.8
# built with OpenSSL 3.5.5 27 Jan 2026

# intermediate configuration
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ecdh_curve X25519MLKEM768:X25519:prime256v1:secp384r1;
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
ssl_prefer_server_ciphers off;

# see also ssl_session_ticket_key alternative to stateful session cache
ssl_session_timeout 1d;
ssl_session_cache shared:MozSSL:10m;  # about 40000 sessions
